import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

interface Props {}

const RegisterHeaderWithSocial = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export {RegisterHeaderWithSocial};

const styles = StyleSheet.create({});
